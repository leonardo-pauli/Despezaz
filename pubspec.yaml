name: despezaz
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  mobx: ^2.3.0
  flutter_mobx: ^2.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2
  flutter:
    sdk: flutter

dev_dependencies:
  mobx_codegen: ^2.6.0
  build_runner: ^2.4.7
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
